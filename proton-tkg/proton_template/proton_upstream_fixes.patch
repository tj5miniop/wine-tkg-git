From 97fcb845a5b7fbf21d57be72662376d15d72fe84 Mon Sep 17 00:00:00 2001
From: Tk-Glitch <ti3nou@gmail.com>
Date: Tue, 01 Sep 2026 01:16:02 +0200
Subject: MORE HAX

--- a/Makefile.in
+++ b/Makefile.in
@@ -636,10 +636,13 @@ WINE_x86_64_DEPENDS = piper vkd3d
 WINE_aarch64_DEPENDS = piper
 
 WINE_i386_LIBDIR = $(WINE_i386_DST)/lib
 WINE_x86_64_LIBDIR = $(WINE_x86_64_DST)/lib
 WINE_aarch64_LIBDIR = $(WINE_aarch64_DST)/lib
+WINE_i386_INCDIR = $(WINE_i386_DST)/include/wine/windows
+WINE_x86_64_INCDIR = $(WINE_x86_64_DST)/include/wine/windows
+WINE_aarch64_INCDIR = $(WINE_aarch64_DST)/include/wine/windows
 
 $(eval $(call rules-source,wine,$(SRCDIR)/wine))
 $(eval $(call rules-wine-requests,wine))
 $(eval $(call rules-wine-tools,wine,i386,unix))
 $(eval $(call rules-wine-tools,wine,x86_64,unix))

--- a/make/rules-makedep.mk
+++ b/make/rules-makedep.mk
@@ -9,10 +9,10 @@
 
 $(2)_x86_64_DEPS := $$(call toupper,$$($(2)_DEPENDS)) $$(call toupper,$$($(2)_x86_64_DEPENDS))
 $(2)_x86_64_INCFLAGS = $$(foreach d,$$($(2)_x86_64_DEPS),-I$$($$(d)_x86_64_INCDIR))
-$(2)_x86_64-windows_LIBFLAGS = $$(foreach d,$$($(2)_x86_64_DEPS),-L$$($$(d)_x86_64_LIBDIR)/$$(x86_64-windows_LIBDIR))
+$(2)_x86_64-windows_LIBFLAGS = $$(foreach d,$$($(2)_x86_64_DEPS),-L$$($$(d)_x86_64_LIBDIR)/wine/x86_64-windows -L$$($$(d)_x86_64_LIBDIR)/$$(x86_64-windows_LIBDIR))
 $(2)_i386_DEPS := $$(call toupper,$$($(2)_DEPENDS)) $$(call toupper,$$($(2)_i386_DEPENDS))
 $(2)_i386_INCFLAGS = $$(foreach d,$$($(2)_i386_DEPS),-I$$($$(d)_i386_INCDIR))
-$(2)_i386-windows_LIBFLAGS = $$(foreach d,$$($(2)_i386_DEPS),-L$$($$(d)_i386_LIBDIR)/$$(i386-windows_LIBDIR))
+$(2)_i386-windows_LIBFLAGS = $$(foreach d,$$($(2)_i386_DEPS),-L$$($$(d)_i386_LIBDIR)/wine/i386-windows -L$$($$(d)_i386_LIBDIR)/$$(i386-windows_LIBDIR))
 $(2)_aarch64_DEPS := $$(call toupper,$$($(2)_DEPENDS)) $$(call toupper,$$($(2)_aarch64_DEPENDS))
 $(2)_aarch64_INCFLAGS = $$(foreach d,$$($(2)_aarch64_DEPS),-I$$($$(d)_aarch64_INCDIR))
 $(2)_aarch64-windows_LIBFLAGS = $$(foreach d,$$($(2)_aarch64_DEPS),-L$$($$(d)_aarch64_LIBDIR)/$$(aarch64-windows_LIBDIR))

--- a/make/rules-common.mk
+++ b/make/rules-common.mk
@@ -98,7 +98,7 @@
 
 $(2)_$(3)_INCFLAGS = $$(foreach d,$$($(2)_$(3)_DEPS),-I$$($$(d)_$(3)_INCDIR))
 $(2)_$(3)_LIBFLAGS = $$(foreach d,$$($(2)_$(3)_DEPS),-L$$($$(d)_$(3)_LIBDIR)/$$($(3)-$(4)_LIBDIR))
-$(2)_$(3)-unix_LIBFLAGS = $$(foreach d,$$($(2)_$(3)_DEPS),-Wl,-rpath-link=$$($$(d)_$(3)_LIBDIR)/$$($(3)-unix_LIBDIR))
+$(2)_$(3)-unix_LIBFLAGS = $$(foreach d,$$($(2)_$(3)_DEPS),-L$$($$(d)_$(3)_LIBDIR)/wine/$(3)-unix -Wl,-rpath-link=$$($$(d)_$(3)_LIBDIR)/$$($(3)-unix_LIBDIR))
 
 # PKG_CONFIG is intentionally never using windows target, as it's missing
 # wrapper scripts in the toolchain, we use PKG_CONFIG_LIBDIR directly

--- a/Makefile.in
+++ b/Makefile.in
@@ -416,10 +416,13 @@ $(eval $(call rules-cmake,glslang,aarch64,unix))
 ## lsteamclient
 ##
 
 LSTEAMCLIENT_DEPENDS = wine
 
+## lsteamclient.so is linked with $(CC) from C++ objects: link libstdc++ explicitly
+lsteamclient_LDFLAGS = -lstdc++
+
 $(eval $(call rules-source,lsteamclient,$(SRCDIR)/lsteamclient))
 $(eval $(call rules-makedep,lsteamclient,i386))
 $(eval $(call rules-makedep,lsteamclient,x86_64))
 $(eval $(call rules-makedep,lsteamclient,aarch64))

--- a/make/rules-makedep.mk
+++ b/make/rules-makedep.mk
@@ -23,6 +23,9 @@
 $$(OBJ)/.$(1)-$(3)-configure: $$(OBJ)/.wine-$$(HOST_ARCH)-tools
	@echo ":: configuring $(1)-$(3)..." >&2
	touch $$($(2)_$(3)_OBJ)/config.status
+	mkdir -p $$($(2)_$(3)_OBJ)/include
+	cp $$(WINE_$(3)_OBJ)/include/config.h $$($(2)_$(3)_OBJ)/include/config.h
+	touch $$($(2)_$(3)_OBJ)/include/stamp-h

	sed -e '1 i\UNIX_LIBS = $$(WINE_$(3)_LIBDIR)/wine/$(3)-unix/ntdll.so\n' \
	    -e '/^all:$$$$/,$$$$c all:' \

Index: a/Makefile.in
===================================================================
@@ -1458,6 +1458,8 @@
 .PHONY: redist
 redist: all
 	mkdir -p $(REDIST_DIR)
+	cp -a $(DST_DIR)/lib/wine/x86_64-unix/wine64 $(DST_DIR)/lib/wine/x86_64-unix/wine
+	cp -a $(DST_DIR)/lib/wine/x86_64-unix/wine64-preloader $(DST_DIR)/lib/wine/x86_64-unix/wine-preloader
 	rsync --delete -arx $(DST_BASE)/ $(REDIST_DIR)
 
 


